# このアプリケーションは主にPythonの標準ライブラリ（tkinter）を使用しています。
# 将来の機能拡張のために必要になる可能性のあるパッケージをコメントとして記載しています。

# GUI関連（現在はtkinterを使用）
# PyQt6>=6.4.0
# customtkinter>=5.2.0

# データ永続化（現在はJSONを使用）
# SQLAlchemy>=2.0.0  # SQLデータベースを使用する場合
# PyYAML>=6.0.1     # YAML形式でデータを保存する場合

# アプリケーションのビルド用
pyinstaller>=6.3.0
